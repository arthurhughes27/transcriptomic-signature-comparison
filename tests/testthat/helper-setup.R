# Sourced automatically by testthat before running tests: loads every
# function in R/ so tests can call them directly. Assumes tests are run
# from the project root (see tests/run_tests.R).
source(fs::path("R", "load_all.R"))
